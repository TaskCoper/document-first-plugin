interface:
  display_name: "Soạn Business Rule"
  short_description: "Soạn Business Rule rõ điều kiện và ngoại lệ"
  default_prompt: "Use $draft-business-rule to turn this policy into a testable Document First Business Rule draft."
dependencies:
  tools:
    - type: "mcp"
      value: "document-first"
      description: "Document First approved-document context"
      transport: "streamable_http"
      url: "https://document-api.vnzdna.com/mcp"
policy:
  allow_implicit_invocation: true
