interface:
  display_name: "Review Document Impact"
  short_description: "Phân tích tác động từ tài liệu hiện tại"
  default_prompt: "Use $review-impact to identify project requirements and code paths affected by this change."
dependencies:
  tools:
    - type: "mcp"
      value: "document-first"
      description: "Document First project document context"
      transport: "streamable_http"
      url: "https://document-api.vnzdna.com/mcp"
policy:
  allow_implicit_invocation: true